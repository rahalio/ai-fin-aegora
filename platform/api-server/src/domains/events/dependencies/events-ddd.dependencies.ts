/**
 * Events DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: events
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * Events Domain Module - Composition root
 *
 * DDD: Composition root for events domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  EventRepositoryAdapter,
} from "@aegora/adapters/events";
import { getIdGeneratorService } from "@aegora/adapters";
import type { AdapterDynamoDBClient } from "@aegora/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetEvent,
  ExecuteIngestEvent,
  ExecuteListEvents,
} from "@aegora/services/events/usecases";
// Ports (interfaces)
import type {
  EventRepository,
} from "@aegora/services/events/ports";

export interface EventsDomainModule {
  repos: {
    events: EventRepository;
  };
  useCases: {
    events: {
      get: ExecuteGetEvent;
      list: ExecuteListEvents;
    };
  };
}

export function buildEventsDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): EventsDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      events?: EventRepository;
    } = {};

    obj.events = new EventRepositoryAdapter(dynamoClient);
    return {
      events: obj.events!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    events: {
      get: new ExecuteGetEvent(executionContext, idGenerator, repos.events),
      list: new ExecuteListEvents(executionContext, idGenerator, repos.events),
    },
  };
  return { repos, useCases: useCases as EventsDomainModule["useCases"] };
}
