/**
 * Alerts Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const createAlert_Body = z
  .object({
    alertId: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
    customerId: z.string().min(1).max(128),
    alertType: z.enum(['geoCluster', 'cardClone', 'other']).optional(),
    status: z.enum([
      'open',
      'investigating',
      'resolved',
      'falsePositive',
      'suppressed',
    ]),
    severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
    slaDeadlineAt: z.string().datetime({ offset: true }).optional(),
    slaBreached: z.boolean().optional(),
    clusterCentroid: z.object({}).partial().passthrough().optional(),
    convexHull: z.object({}).partial().passthrough().optional(),
    priorVenues: z.array(z.string()).optional(),
    createdAt: z.string().datetime({ offset: true }),
    updatedAt: z.string().datetime({ offset: true }).optional(),
    raisedAt: z.string().datetime({ offset: true }),
  })
  .passthrough();
const suppressAlert_Body = z
  .object({ note: z.string(), reasonCode: z.string() })
  .partial()
  .passthrough();

export const schemas: any = {
  createAlert_Body,
  suppressAlert_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v1/alerts',
    alias: 'listAlerts',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z
          .enum([
            'open',
            'investigating',
            'resolved',
            'falsePositive',
            'suppressed',
          ])
          .optional(),
      },
      {
        name: 'customerId',
        type: 'Query',
        schema: z.string().min(1).max(128).optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  alertId: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
                  customerId: z.string().min(1).max(128),
                  alertType: z
                    .enum(['geoCluster', 'cardClone', 'other'])
                    .optional(),
                  status: z.enum([
                    'open',
                    'investigating',
                    'resolved',
                    'falsePositive',
                    'suppressed',
                  ]),
                  severity: z
                    .enum(['low', 'medium', 'high', 'critical'])
                    .optional(),
                  slaDeadlineAt: z
                    .string()
                    .datetime({ offset: true })
                    .optional(),
                  slaBreached: z.boolean().optional(),
                  clusterCentroid: z
                    .object({})
                    .partial()
                    .passthrough()
                    .optional(),
                  convexHull: z.object({}).partial().passthrough().optional(),
                  priorVenues: z.array(z.string()).optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }).optional(),
                  raisedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v1/alerts',
    alias: 'createAlert',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: createAlert_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            alertId: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
            customerId: z.string().min(1).max(128),
            alertType: z.enum(['geoCluster', 'cardClone', 'other']).optional(),
            status: z.enum([
              'open',
              'investigating',
              'resolved',
              'falsePositive',
              'suppressed',
            ]),
            severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
            slaDeadlineAt: z.string().datetime({ offset: true }).optional(),
            slaBreached: z.boolean().optional(),
            clusterCentroid: z.object({}).partial().passthrough().optional(),
            convexHull: z.object({}).partial().passthrough().optional(),
            priorVenues: z.array(z.string()).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
            raisedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'get',
    path: '/v1/alerts/:alertId',
    alias: 'getAlert',
    requestFormat: 'json',
    parameters: [
      {
        name: 'alertId',
        type: 'Path',
        schema: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            alertId: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
            customerId: z.string().min(1).max(128),
            alertType: z.enum(['geoCluster', 'cardClone', 'other']).optional(),
            status: z.enum([
              'open',
              'investigating',
              'resolved',
              'falsePositive',
              'suppressed',
            ]),
            severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
            slaDeadlineAt: z.string().datetime({ offset: true }).optional(),
            slaBreached: z.boolean().optional(),
            clusterCentroid: z.object({}).partial().passthrough().optional(),
            convexHull: z.object({}).partial().passthrough().optional(),
            priorVenues: z.array(z.string()).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
            raisedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v1/alerts/:alertId/escalate',
    alias: 'escalateAlert',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: suppressAlert_Body,
      },
      {
        name: 'alertId',
        type: 'Path',
        schema: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            alertId: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
            customerId: z.string().min(1).max(128),
            alertType: z.enum(['geoCluster', 'cardClone', 'other']).optional(),
            status: z.enum([
              'open',
              'investigating',
              'resolved',
              'falsePositive',
              'suppressed',
            ]),
            severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
            slaDeadlineAt: z.string().datetime({ offset: true }).optional(),
            slaBreached: z.boolean().optional(),
            clusterCentroid: z.object({}).partial().passthrough().optional(),
            convexHull: z.object({}).partial().passthrough().optional(),
            priorVenues: z.array(z.string()).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
            raisedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
  {
    method: 'post',
    path: '/v1/alerts/:alertId/suppress',
    alias: 'suppressAlert',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: suppressAlert_Body,
      },
      {
        name: 'alertId',
        type: 'Path',
        schema: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            alertId: z.string().regex(/^alt_[0-9A-HJKMNP-TV-Z]{26}$/),
            customerId: z.string().min(1).max(128),
            alertType: z.enum(['geoCluster', 'cardClone', 'other']).optional(),
            status: z.enum([
              'open',
              'investigating',
              'resolved',
              'falsePositive',
              'suppressed',
            ]),
            severity: z.enum(['low', 'medium', 'high', 'critical']).optional(),
            slaDeadlineAt: z.string().datetime({ offset: true }).optional(),
            slaBreached: z.boolean().optional(),
            clusterCentroid: z.object({}).partial().passthrough().optional(),
            convexHull: z.object({}).partial().passthrough().optional(),
            priorVenues: z.array(z.string()).optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }).optional(),
            raisedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
  },
]);

export const api: any = new Zodios(
  'https://api.ddd-codegen-starter.local',
  endpoints
);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
