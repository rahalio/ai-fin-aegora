/**
 * ExecuteCloseCase - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteCloseCase Use Case
 *
 * DDD: Application use case for get Close.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { CloseCaseInput, CloseCaseOutput } from "../dto/close.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegora/services/_shared/index.js";
import type { CloseRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteCloseCase {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly close: CloseRepository
  ) {}

  async execute(input: CloseCaseInput): Promise<CloseCaseOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.casesId();

    // All field names extracted from OpenAPI spec: resolution, siemTicketId
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).caseId) {
      throw new ValidationError("caseId is required");
    }
    const caseId = (input as any).caseId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.close.closeCase({ caseId: caseId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${caseId}`);
    }

    return entity as CloseCaseOutput;
  }
}
