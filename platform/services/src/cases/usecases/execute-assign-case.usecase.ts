/**
 * ExecuteAssignCase - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: cases
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAssignCase Use Case
 *
 * DDD: Application use case for get Assign.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AssignCaseInput, AssignCaseOutput } from "../dto/assign.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegora/services/_shared/index.js";
import type { AssignRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAssignCase {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly assign: AssignRepository
  ) {}

  async execute(input: AssignCaseInput): Promise<AssignCaseOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.casesId();

    // All field names extracted from OpenAPI spec: assignedTo
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).caseId) {
      throw new ValidationError("caseId is required");
    }
    const caseId = (input as any).caseId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.assign.assignCase({ caseId: caseId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${caseId}`);
    }

    return entity as AssignCaseOutput;
  }
}
