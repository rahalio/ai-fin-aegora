/**
 * ExecuteOverrideOffer - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: offers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOverrideOffer Use Case
 *
 * DDD: Application use case for get Override.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OverrideOfferInput, OverrideOfferOutput } from "../dto/override.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegora/services/_shared/index.js";
import type { OverrideRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteOverrideOffer {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly override: OverrideRepository
  ) {}

  async execute(input: OverrideOfferInput): Promise<OverrideOfferOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.offersId();

    // All field names extracted from OpenAPI spec: action, note, reasonCode
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).offerId) {
      throw new ValidationError("offerId is required");
    }
    const offerId = (input as any).offerId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.override.overrideOffer({ offerId: offerId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${offerId}`);
    }

    return entity as OverrideOfferOutput;
  }
}
