/**
 * ExecuteSuppressAlert - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: alerts
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteSuppressAlert Use Case
 *
 * DDD: Application use case for get Suppress.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { SuppressAlertInput, SuppressAlertOutput } from "../dto/suppress.dto";
import type { ExecutionContextService, IdGeneratorService } from "@aegora/services/_shared/index.js";
import type { SuppressRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteSuppressAlert {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly suppress: SuppressRepository
  ) {}

  async execute(input: SuppressAlertInput): Promise<SuppressAlertOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.alertsId();

    // All field names extracted from OpenAPI spec: note, reasonCode
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).alertId) {
      throw new ValidationError("alertId is required");
    }
    const alertId = (input as any).alertId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.suppress.suppressAlert({ alertId: alertId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${alertId}`);
    }

    return entity as SuppressAlertOutput;
  }
}
