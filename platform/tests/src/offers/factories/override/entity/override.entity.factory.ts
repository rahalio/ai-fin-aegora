/**
 * Override Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: offers
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { OverrideEntity } from "@aegora/core/offers/models/index.js";
import { offersSchemas } from "@aegora/core/offers";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "offers";

export function createOverride(overrides: Partial<OverrideEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    action: overrides.action ?? "veto",
    offerId: overrides.offerId ?? `ofr_${ulid().toLowerCase()}`,
    overrideId: overrides.overrideId ?? `ovr_${ulid().toLowerCase()}`,
    reasonCode: overrides.reasonCode ?? faker.lorem.word(),
    ...overrides,
  };

  return offersSchemas.Override.parse(raw);
}

export function overrideBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createOverride(overrides));
}
