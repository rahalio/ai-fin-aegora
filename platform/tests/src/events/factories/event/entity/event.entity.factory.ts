/**
 * Event Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: events
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { EventEntity } from "@aegora/core/events/models/index.js";
import { eventsSchemas } from "@aegora/core/events";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "events";

export function createEvent(overrides: Partial<EventEntity> = {}) {
  const raw = {
    id: overrides.id ?? `${DOMAIN_PREFIX}_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    customerId: overrides.customerId ?? faker.string.uuid(),
    eventId: overrides.eventId ?? `evt_${ulid().toLowerCase()}`,
    eventType: overrides.eventType ?? "cardAuthorisation",
    occurredAt: overrides.occurredAt ?? new Date().toISOString(),
    purpose: overrides.purpose ?? "help",
    ...overrides,
  };

  return eventsSchemas.Event.parse(raw);
}

export function eventBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createEvent(overrides));
}
