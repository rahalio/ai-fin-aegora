/**
 * HoldRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: holds
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * HoldRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements HoldRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  HoldRepository,
} from "@aegora/services/holds";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { HoldRepositoryDdb } from "./hold-repository.ddb.js";

/**
 * HoldRepositoryAdapter
 *
 * Implements HoldRepository port for infrastructure layer.
 */
export class HoldRepositoryAdapter implements HoldRepository {
  private readonly ddb: HoldRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new HoldRepositoryDdb(this.dynamoClient);
  }

  async listHolds(input: Parameters<HoldRepository['listHolds']>[0]): Promise<Awaited<ReturnType<HoldRepository['listHolds']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listHolds(input);
  }

  async createHold(input: Parameters<HoldRepository['createHold']>[0]): Promise<Awaited<ReturnType<HoldRepository['createHold']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.createHold(input);
  }

  async getHold(input: Parameters<HoldRepository['getHold']>[0]): Promise<Awaited<ReturnType<HoldRepository['getHold']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getHold(input);
  }
}
